export async function scoreJobs(jobs: any[]) {
	// Placeholder implementation: currently returns jobs unchanged.
	// Replace with actual Gemini API scoring/filtering later.
	return jobs;
}

export default {
	scoreJobs,
};
